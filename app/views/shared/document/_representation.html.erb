<div>
  <% if document.file.representable? %>
    <p><%= image_tag document.file.representation(resize_to_limit: [500, 500]) %></p>
  <% else %>
    <p><%= t('.no_representation') %></p>
  <% end %>
</div>
